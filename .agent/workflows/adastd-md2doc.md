# WORKFLOW: /adastd-md2doc

This workflow handles the conversion of Markdown files to MS Word documents using Adasoft standards.

## 🚀 EXECUTION STEPS

### 1. Validation
- Check if the source path exists.
- Check if the source contains `.md` files.
- Verify template existence at `../template/adasoft-template.docx`.

### 2. Conversion
- Execute the conversion script:
  ```powershell
  python "../skills/adastd-md2doc/scripts/md2doc.py" "{{source}}" "{{target}}"
  ```

### 3. Verification
- Confirm that `.docx` files have been created in the target directory.
- Report the results to the user.

## 📋 COMMAND SYNTAX
`/adastd-md2doc <source_path> <target_directory>`

---
*Generated by Antigravity*
